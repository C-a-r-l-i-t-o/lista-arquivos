#include <stdio.h>
#include <stdlib.h>

int aloca()
{
// aloca um vetor de 'tam' inteiros, retornando 1 se foi possıvel
// alocar e 0 se nao foi
// o paramentro 'ptr' deve retornar com o apontador para a
//  ́area alocada

struct Alocacao{
int i;
int tam;
int *ptr;

scanf("%d", int* &ptr);

alocacao = (int*)malloc(sizeof(&ptr));
if(tam == NULL);
    printf("Fail of memory.\n");

    return 1;
 


    };
}